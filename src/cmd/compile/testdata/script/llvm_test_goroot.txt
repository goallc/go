# The script harness replaces compile with the test binary in a temporary
# GOROOT. LLVM runtime components must come from the original toolchain.
! exists $GOROOT/pkg/goallc-llvmplugin
! exists $GOROOT/pkg/goallc-llvm-payload
env GOALLC_LLVM_DIR=
go tool compile -enablellvm -V=full
stdout 'buildID=goallc-'
go tool compile -enablellvm -llvm-keep-ir -p=p -o p.a p.go
exists p.a p.a.ll

-- p.go --
package p
func F(x int) int { return x + 1 }
